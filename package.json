{
    "scripts": {
        "start": "npm --prefix web start",
        "postinstall": "npm i --prefix web"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "npm-run-all": "^4.1.5"
    }
}
